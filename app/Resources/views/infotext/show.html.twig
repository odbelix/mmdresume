{% extends "ResumeBundle:Default:panel-backend.html.twig" %}

{% block maincontent %}
<div class="row">
    <div class="col-lg-12">
    <h1 class="page-header">Textos de Ayuda</h1>
    </div>
    <!-- /.col-lg-12 -->
</div>
<div class="row">
    <div class="col-lg-6">
    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ infotext.id }}</td>
            </tr>
            <tr>
                <th>Nombre Corto</th>
                <td>{{ infotext.shortname }}</td>
            </tr>
            <tr>
                <th>Seccción</th>
                <td>{{ infotext.section }}</td>
            </tr>
            <tr>
                <th>Texto</th>
                <td>{{ infotext.text }}</td>
            </tr>
            <tr>
              <td colspan="4">
                <div class="btn-group button-in-table" role="group" aria-label="...">
                    <a class="btn btn-default" href="{{ path('panel_infotext_index') }}">Volver</a>
                    <a class="btn btn-default" href="{{ path('panel_infotext_edit', { 'id': infotext.id }) }}">Editar</a>
                </div>
              </td>
            </tr>
        </tbody>
    </table>
    </div>
    <div class="col-lg-6"
            {{ form_start(delete_form) }}
                <input class="btb btn-danger" type="submit" value="Eliminar">
            {{ form_end(delete_form) }}
    </div>
</div>
{% endblock %}
