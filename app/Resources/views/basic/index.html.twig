{# extends 'base.html.twig' #}
{% extends "ResumeBundle:Default:panel-backend.html.twig" %}

{% block maincontent %}
<div class="row">
    <div class="col-lg-12">
    <h1 class="page-header">Título Docente Educación Básica</h1>
    </div>
    <!-- /.col-lg-12 -->
</div>
<!-- /.row -->
<div class="row">
  <div class="col-lg-12">
      <div class="panel panel-default">
        <div class="panel-heading">Lista de <b>Título Docente Educación Básica</b> incluidos en la plataforma para la <b>creación de Trabajos y CV</b>
        <a class="btn btn-default" href="{{ path('panel_basic_new') }}">Nuevo</a>
        </div>
        <div class="panel-body">
    {% include "ResumeBundle:Utils:search-table-searchbar.html.twig" %}
          <table class="table table-bordered results">
        <thead>
            <tr>
                <th>#</th>
                <th>Nombre</th>
                <th>Opciones</th>
            </tr>
            {% set columns = 3 %}
            {% include "ResumeBundle:Utils:search-table-trresult.html.twig" %}
        </thead>
        <tbody>
        {% set count = 1 %}
        {% for basic in basics %}
            <tr>
                <td><a href="{{ path('panel_basic_show', { 'id': basic.id }) }}">{{ count }}</a></td>
                <td>{{ basic.name }}</td>
                <td>
                    <a class="btn btn-default" href="{{ path('panel_basic_edit', { 'id': basic.id }) }}">Editar</a>
                </td>
            </tr>
            {% set count = count + 1 %}
        {% endfor %}
            <tr>
              <td colspan="3">
                <a class="btn btn-default" href="{{ path('panel_basic_new') }}">Nuevo</a>
              </td>
            </tr>
        </tbody>
    </table>
    </div>
  </div>
</div>
</div>

{% endblock %}


{% block scriptpage %}
<script src="{{asset('js/search-bar-tables.js')}}"></script>
{% endblock %}
