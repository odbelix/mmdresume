 {% extends "ResumeBundle:Default:panel-backend.html.twig" %}

{% block csspage %}
<link href="{{asset('css/bootstrap-datepicker3.css')}}" rel="stylesheet" type="text/css">
{% endblock%}



{% block maincontent %}

<div class="row">
    <div class="col-lg-12">
    <h1 class="page-header">Trabajos</h1>
    </div>
    <!-- /.col-lg-12 -->
</div>
<!-- /.row -->
<div class="row">
  <div class="col-lg-12">
      <div class="panel panel-default">
        <div class="panel-heading">Editando Información del Trabajo</div>
        <div class="panel-body">
        <div class="row">
          <div class="col-lg-6">
          {{ form_start(edit_form,{'attr': { 'role': 'form' } }) }}
          <div class="form-group">
            <label>Nombre</label>
            <p class="text-danger">{{ form_errors(edit_form.name) }}</p>
                {{ form_widget(edit_form.name,{ 'attr': {'class': 'form-control'}}) }}
          </div>
          <div class="form-group">
            <label>Detalle</label>
          <p class="text-danger">{{ form_errors(edit_form.detail) }}</p>
	        {{ form_widget(edit_form.detail,{ 'attr': {'class': 'form-control','row':'3'}}) }}
          </div>

          <div class="form-group">
            <label>Fecha Inicio</label>
          <p class="text-danger">{{ form_errors(edit_form.startjob) }}</p>
	{{ form_widget(edit_form.startjob,{ 'attr': {'class': 'form-control'}}) }}
          </div>
          <div class="form-group">
            <label>Fecha Termino</label>
          <p class="text-danger">{{ form_errors(edit_form.endjob) }}</p>
	{{ form_widget(edit_form.endjob,{ 'attr': {'class': 'form-control'}}) }}
          </div>
          <div class="form-group">
            <label>Horas</label>
          <p class="text-danger">{{ form_errors(edit_form.hours) }}</p>
	{{ form_widget(edit_form.hours,{ 'attr': {'class': 'form-control'}}) }}
          </div>
          <div class="form-group">
            <label>Especialidad</label>
          <p class="text-danger">{{ form_errors(edit_form.speciality) }}</p>
	{{ form_widget(edit_form.speciality,{ 'attr': {'class': 'form-control'}}) }}
          </div>

          <div class="form-group">
            <label>Establecimiento</label>
          <p class="text-danger">{{ form_errors(edit_form.workplace) }}</p>
	{{ form_widget(edit_form.workplace,{ 'attr': {'class': 'form-control'}}) }}
          </div>
          <div class="btn-group button-in-table" role="group" aria-label="...">
                <a class="btn btn-default" href="{{ path('panel_job_index') }}">Volver</a>
                <input class="btn btn-default" type="submit" value="Editar" />
          </div>
    </div>
    {{ form_end(edit_form) }}

    <div class="col-lg-6">
    <!-- DATA DISABLED FOR EDITION -->
    <table class="table table-bordered table-hover">
      <thead>
      <tr>
        <td colspan="2">Gestión del Trabajo</td>
      </tr>
      </thea>
      <tbody>
      <tr>
        <th>Creado por</th>
        <td>{{ job.username }} el <b>{% if job.created %}{{ job.created|date('Y-m-d') }}{% endif %}</b></td>
      </tr>
      <tr>
        <th>Ultima edición por</th>
        <td>{{ job.lastusername }} el <b>{% if job.lastupdate %}{{ job.lastupdate|date('Y-m-d') }}{% endif %}</b></td>
      </tr>
      <tr>
        <th>Versión del Trabajo</th>
        <td><b>{{ job.version }}</b></td>
      </tr>
      </tbody>
    </table>

    </div>
    </div>

      </div>
    </div>
    <!-- /.panel panel-default

    </div>
    </div>
    <!-- /.col-lg-12 /.row -->

{% endblock %}

{% block scriptpage %}
<script src="{{asset('js/bootstrap-datepicker.js')}}"></script>
<script src="{{asset('js/bootstrap-datepicker.es.min.js')}}"></script>
<script type="text/javascript">
$(function () {
  $('#job_startjob').datepicker({
    language: 'es'
  });
  $('#job_endjob').datepicker({
    language: 'es'
  });
});
</script>
</script>
{% endblock%}
